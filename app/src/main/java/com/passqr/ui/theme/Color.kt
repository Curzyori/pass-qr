package com.passqr.ui.theme

import androidx.compose.ui.graphics.Color

// DESIGN.md palette
val CoralPrimary    = Color(0xFFCC785C)   // #cc785c — Coral/Rust
val CoralDark       = Color(0xFFB5654A)   // Darker shade for pressed state
val CoralLight      = Color(0xFFE8A48C)   // Lighter tint for surfaces

val CanvasCream     = Color(0xFFFAF9F5)   // #faf9f5 — Tinted Cream background
val SurfaceElevated = Color(0xFFEFE9DE)   // #efe9de — Card / elevated surface
val SurfaceDark     = Color(0xFF181715)   // #181715 — Deep Navy/Black surface
val InkDeep         = Color(0xFF141413)   // #141413 — Deep Ink text

// On-colors
val OnPrimary       = Color(0xFFFFFFFF)   // White text on coral
val OnCanvas        = InkDeep             // Dark text on cream canvas
val OnSurface       = Color(0xFFFFFFFF)   // White text on dark surface
val OnSurfaceElevated = InkDeep           // Dark text on elevated card

// Utility
val DividerColor    = Color(0xFFD6CFC3)   // Subtle divider on cream
val ErrorRed        = Color(0xFFD32F2F)
